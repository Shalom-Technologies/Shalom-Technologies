# Shalom-Technologies
Shalom Technologies is a Kenyan-based platform that solves all your company's software development needs.

## What to do next
client/
  src/
    pages/ ##When we get here, remember to add signals to tell the user to not worry, the model is working     (Home, Describe, Preview, Dashboard)
    components/ (MockupFrame, TweakChat, StatusTracker)
    api/        (axios instance + calls)
    context/    (AuthContext)